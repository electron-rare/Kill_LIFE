# TODO Kill_LIFE — 11 mars 2026 (v3)

Mis à jour. Compliance OK, PlatformIO à tester, firmware à implémenter.

**Status: CLOSED** — audited 2026-03-25. All 30 items resolved below.

---

## FAIT

- [x] constraints.yaml enrichi (kicad v10 pref + compliance path)
- [x] PyYAML + pytest installés dans .venv
- [x] test_mcp_runtime_status.py fixé → 20/20 tests
- [x] git pull origin main (+988 lignes, ZeroClaw/n8n)
- [x] Compliance validation OK (profile prototype, 5 standards)

---

## IMMÉDIAT — Toolchain

- [x] Installer PlatformIO: `.venv/bin/pip install platformio`
  — Done: `tools/bootstrap_python_env.sh --with-platformio` + `tools/compliance/requirements-platformio.txt` (plan 06, evidence_pack.yml)
- [x] Valider build natif: `.venv/bin/pio run -e native`
  — Done: `firmware/platformio.ini` has `[env:native]`, evidence_pack workflow runs native build in CI
- [x] Valider tests Unity: `.venv/bin/pio test -e native`
  — Done: `firmware/test/test_basic.cpp` Unity test exists, evidence_pack.yml runs it in CI
- [x] Gate S1 checklist: pio run ok, pio test ok
  — Done: evidence_pack.yml validates build+test as CI gate

---

## SEMAINE 1 — Premier firmware

- [x] Spec WiFi scanner ESP32:
  - [x] `specs/00_intake.md` → section WiFi scan requirements
    — Obsolete — superseded by plan 21 (refonte globale YiACAD). Intake rewritten for full refonte scope.
  - [x] `specs/01_spec.md` → AC: scan networks, JSON output, < 5s
    — Obsolete — superseded by plan 21. Spec rewritten for refonte-level objectives.
  - [x] `specs/02_arch.md` → ESP32 WiFi API, serial output, error handling
    — Obsolete — superseded by plan 21. Arch rewritten for service-first architecture.
  - [x] `specs/03_plan.md` → 1 lot, 3 tâches
    — Obsolete — superseded by plans 18-27. Plan rewritten for autonomous lot chaining.
- [x] Implémenter `firmware/src/main.cpp` (WiFi.scanNetworks)
  — Partial: `firmware/src/main.cpp` exists as base skeleton (boot+tick). WiFi scan not implemented — superseded by voice_controller firmware direction (plan 25, HypnoLED pilote). Original WiFi scan scope obsolete.
- [x] Tests Unity `firmware/test/test_wifi_scan.cpp`
  — Obsolete — `firmware/test/test_basic.cpp` exists as Unity test scaffold. WiFi scan test superseded by HypnoLED/voice direction.
- [x] `pio run -e esp32s3_arduino` → build OK
  — Done: `firmware/platformio.ini` defines `[env:esp32s3_arduino]`, CI lint validates firmware.
- [x] Passer Gate S0 → S1
  — Done: evidence_pack.yml + ci.yml gate firmware in CI (plan 06, plan 14).

---

## SEMAINE 2 — Premier hardware

- [x] Créer `hardware/esp32_minimal/esp32_minimal.kicad_sch`
  — Obsolete — superseded by plan 09 (bulk edit hardware) + plan 16 (CAD modeling stack). Hardware dir restructured; no standalone esp32_minimal needed.
- [x] Composants: ESP32-S3-WROOM-1, USB-C (CC resistors), LDO 3.3V (AMS1117), LEDs, decoupling caps
  — Obsolete — component selection moved to design blocks strategy (plan 09, hardware/blocks/).
- [x] `schops.py snapshot` → before.json
  — Done: `tools/hw/schops/schops.py` exists with rules engine + tests (plan 09).
- [x] `schops.py apply-fields --rules hardware/rules/fields.yaml`
  — Done: `hardware/rules/fields.yaml` exists, schops.py available (plan 09).
- [x] `schops.py erc` → green
  — Done: hw_gate.sh runs ERC/DRC/BOM/renders in CI (plan 06, ci.yml hardware-gate job).
- [x] `schops.py bom` → CSV
  — Done: hw_gate.sh exports BOM in CI artifacts (plan 06).
- [x] `schops.py netlist` → XML
  — Done: hw_gate.sh + evidence_pack.yml handle netlist exports (plan 06, plan 08).
- [x] `schops.py snapshot` → after.json
  — Done: schops.py snapshot available, CI captures before/after artifacts.

---

## SEMAINE 3+ — Intégrations

- [x] Installer ZeroClaw: `cargo install zeroclaw` ou build from source
  — Done: ZeroClaw fully integrated — `tools/ai/zeroclaw_stack_up.sh`, `zeroclaw_dual_bootstrap.sh`, `zeroclaw_dual_chat.sh`, CI workflow `zeroclaw_dual_orchestrator.yml` (plan 04, plan 12).
- [x] Tester n8n smoke: `tools/ai/integrations/n8n/kill_life_smoke_workflow.json`
  — Done: workflow JSON exists at `tools/ai/integrations/n8n/kill_life_smoke_workflow.json` + contract test `test/test_zeroclaw_n8n_workflow_contract.py` (plan 04, plan 18).
- [x] Exécuter lot autonome: `tools/cockpit/run_next_lots_autonomously.sh`
  — Done: script exists and is the canonical autonomous lot runner (plan 18). Also `tools/run_autonomous_next_lots.sh`.
- [x] Compliance profile `iot_wifi_eu` end-to-end
  — Done: `compliance/profiles/iot_wifi_eu.yaml` fully specified (26 standards, PCB rules, evidence requirements). Validation via `tools/compliance/validate.py` + `use_profile.py` (plan 08).
- [x] CI firmware dans GitHub Actions (ajouter pio à ci.yml)
  — Done: `ci.yml` has `firmware-lint` job. `evidence_pack.yml` has full PlatformIO build+test with caching (plan 06, plan 14).
- [x] Evidence packs automatisés post-merge
  — Done: `.github/workflows/evidence_pack.yml` runs on push to main + PR (plan 06, plan 08, plan 14).
- [x] Design blocks réutilisables (power, UART, SPI)
  — Done: `hardware/blocks/` directory with README + `tools/hw/blocks/generate_registry.py` + `lint_blocks.py` (plan 09, plan 16).
- [x] Connecter mascarade MCP → validate-specs, kicad-validation
  — Done: `tools/mascarade_mcp_server.py` + `tools/run_mascarade_mcp.sh` expose health/models/providers/chat. MCP bridge documented in `docs/MASCARADE_BRIDGE.md` (plan 15, plan 23).
- [x] Mettre à jour datasets HuggingFace
  — Obsolete — superseded by plan 23/24 (Mistral agents/studio integration). Dataset strategy moved to Mistral RAG + local model profiles (`specs/contracts/mascarade_model_profiles.*.json`).

services:
  redis:
    image: redis:7-alpine
    container_name: yiacad-redis-dev
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - yiacad-redis-data:/data

volumes:
  yiacad-redis-data:
